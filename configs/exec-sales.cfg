! Exec-Sales access switch
! VLAN 100 management interface — confirmed (see docs/screenshots/12, 14)

interface vlan 100
 ip address 192.168.100.2 255.255.255.0
 no shutdown

! Verified via 'show ip interface brief':
!   Vlan100   192.168.100.2   YES manual up   down

! VLAN database — confirmed via CLI session
! (docs/screenshots/36-exec-sales-vlan-name-table.png,
!  docs/screenshots/37-exec-sales-vlan-creation-cli.png)
vlan 20
 name Executive
vlan 21
 name Sales
vlan 40
 name Printing
vlan 50
 name Telephony
vlan 100
 name Administration

! Uplink trunk to core (Multilayer Switch0 Gi1/0/2) — STILL INFERRED.
! The core-side of this trunk is confirmed in configs/multilayer-switch0.cfg
! (Gi1/0/2, native vlan 100, allowed 20,21,40,50) but the matching
! 'switchport trunk ...' commands on Exec-Sales itself still haven't been
! captured in a screenshot (only the VLAN database creation has). Apply/
! verify:
!
! interface g0/1
!  switchport mode trunk
!  switchport trunk native vlan 100
!  switchport trunk allowed vlan 20,21,40,50,100
